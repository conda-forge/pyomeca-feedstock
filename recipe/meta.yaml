{% set owner = "pyomeca" %}
{% set name = "pyomeca" %}
{% set version = "2020.0.2" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/{{ owner }}/{{ name }}/archive/{{ version }}.tar.gz
  sha256: bc3c2d901e071ecf85458fdb58a21a1c0019f96e9b32354e56f00e99ee2c8a34

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - setuptools
    - python >=3
    - pip
  run:
    - python
    - {{ pin_compatible('numpy') }}
    - pandas
    - matplotlib-base
    - scipy
    - {{ pin_compatible('ezc3d') }}
    - xlrd
    - xarray
    - bottleneck

test:
  imports:
    - pyomeca

about:
  home: http://github.com/{{ owner }}/{{ name }}
  license: Apache-2.0
  license_file: LICENSE.md
  summary: Pyomeca is a python library allowing to carry out a complete biomechanical analysis; in a simple, logical and concise way
  description: |
    Python toolbox for biomechanics analysis
  doc_url: https://pyomeca.github.io
  dev_url: https://github.com/{{ owner }}/{{ name }}

extra:
  recipe-maintainers:
    - romainmartinez
    - pariterre
